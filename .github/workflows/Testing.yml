name: This is testing pipeline
on: 
  push:

jobs:
  Basics:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"
        
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
        
      - name: Test the script
        run: |
          echo "abbny.com" | python main.py

  test2:
    steps:
      - name: only testing command
      run: |
      Hello in DevOps Wordl!
      this only test statement